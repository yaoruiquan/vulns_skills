# NCC 字段映射表

## 1. 当前已确认的页面字段

根据 `assets/4填表1.png`、`assets/5填表2.png`、`assets/6填表3.png`，页面可见字段如下：

- 是否为原创漏洞
- 发现日期
- 漏洞类型
- 影响对象
- 漏洞厂商
- 影响组件
- 影响版本
- 漏洞名称
- 漏洞详细分类
- 漏洞 URL
- 漏洞描述
- 漏洞危害
- 修复方案
- 修复方案说明
- 漏洞附件

这些字段名已经确认，但实际下拉选项值和 MCP `uid` 仍需用运行时快照确定。

## 2. `form_context.json` 字段

浏览器填表阶段只读取 `/tmp/vulns-skills/phase2-ncc-report/form-contexts/YYYY-MM/DAS-ID/form_context.json`。准备阶段默认先生成 `NCC-<漏洞名>` 材料目录和 NCC Word，再从 NCC Word 生成运行时 JSON。`extract_vuln_data.py` 是底层提取脚本，不直接作为浏览器阶段的数据源；运行时 JSON 不写入正式提交材料目录。

| 字段 | docx 来源 | 说明 |
|----------|-----------|------|
| `title` | `漏洞名称` | 对应页面“漏洞名称” |
| `description` | `漏洞描述`、`漏洞简介` | 对应页面“漏洞描述” |
| `impact` | `漏洞危害` 或准备阶段 websearch 补全 | 对应页面“漏洞危害”，禁止填写“见附件” |
| `vuln_type` | `漏洞类型` | 仅作为类型判断依据，页面实际选项需人工确认一次 |
| `business_type` | 固定规则 | 对应页面“漏洞业务类型/漏洞类型”，固定为 `通用型漏洞` |
| `detail_category` | `漏洞详细分类`、`漏洞类型`、标题、描述综合判断 | 对应页面“漏洞详细分类”；必须是下方 NCC 允许选项之一 |
| `target_type` | `影响对象类型`、标题、产品、漏洞类型推断 | 对应页面“影响对象”；必须是下方 NCC 允许选项之一 |
| `vendor_country` | `厂商所属国家`、`产品厂商国家`、厂商/产品名推断 | 对应厂商国家/地区下拉；无法判断时默认“中国大陆” |
| `unit_name` | `漏洞厂商`、`影响厂商`、`厂商名称`、标题推导 | 对应页面“漏洞厂商”；原字段为“其他”时从标题/产品推导，如 `xl2tpd` |
| `affected_product` | `影响产品`、`受影响实体`、`产品名称` | 对应页面“影响组件” |
| `version` | `影响版本`、`受影响实体版本号`、`版本号` | 对应页面“影响版本”；会从“已在 1.3.20 版本复现”中提取 `1.3.20` |
| `url` | `漏洞URL`、`漏洞定位` | 对应页面“漏洞 URL” |
| `verification` | `漏洞验证过程`、`验证过程`、`漏洞验证` | 可作为补充说明 |
| `poc_text` | `SQL注入Poc`、`完整PoC描述`、`漏洞验证过程` | 仅 `detail_category=SQL注入` 时提取 Word 中已有的原始 HTTP 请求或 curl 命令并写入 CodeMirror；原始 HTTP 请求必须保持 `GET /path HTTP/1.1` + 多行 Header 的格式，脚本会修复 Word 提取造成的 `HTTP/1.1Host:`、`Host:... User-Agent:` 等换行丢失；没有现成 PoC 时返回“见附件”，禁止根据 URL/请求方式/漏洞分析自动拼接 |
| `temporary_solution` | `临时解决方案` 或准备阶段 websearch 补全 | 对应“修复方案说明”候选内容，禁止填写“见附件” |
| `formal_solution` | `正式解决方案`、`修复方案` | 对应“修复方案说明”候选内容 |
| `security_guidance` | 准备阶段检索策略 | 记录漏洞危害/修复方案的来源、查询词、检索结果和兜底策略 |
| `submit_date` | `提交日期` | 仅供参考，不直接当作“发现日期” |
| `is_0day` | 启动确认参数 `--is-0day-original` | 对应页面“是否0Day漏洞”，必须在生成 JSON 前由用户明确为“是”或“否”，并与 `is_original` 绑定一致；不从 Word 或 `.env` 兜底 |
| `is_original` | 启动确认参数 `--is-0day-original` | 对应页面“是否原创/是否为原创漏洞”，必须在生成 JSON 前由用户明确为“是”或“否”，并与 `is_0day` 绑定一致；不从 Word 或 `.env` 兜底 |
| `docx_path` | 材料目录 | 当前使用的源 `docx` |
| `upload_zip_path` | CNVD 材料 `exp/*.zip` / `poc/*.zip`，或无现成 zip 时的 CNVD 材料目录自动打包结果 | 复制/生成到 `/tmp/vulns-skills/phase2-ncc-report/upload-zips/<DAS-ID>/NCC-*.zip` 后用于上传 |
| `upload_zip_basename` / `upload_zip_dir` / `upload_zip_exists` | 运行时 zip | 上传前校验字段；必须使用 `upload_zip_path` 的原值，不要手动改空格、短横线或下划线 |
| `upload_zip_prepared_path` | 运行时 zip | ffmpeg 压缩前的运行时 `NCC-*.zip` |
| `upload_zip_size_mb` | 运行时 zip | 最终 `upload_zip_path` 的大小，单位 MiB |
| `upload_zip_limit_mb` | `.env` | NCC 上传限制，默认 `50` |
| `upload_zip_within_limit` | 脚本判断 | 最终 zip 是否不超过限制 |
| `upload_zip_compressed` | 脚本判断 | 是否生成并改用 `NCC-*-compressed.zip` |
| `upload_zip_compression` | 脚本判断 | ffmpeg 压缩详情、使用档位和警告 |
| `upload_zip_source_path` | CNVD 原始 zip | 仅用于追溯，不直接上传 |
| `screenshot_paths` | `exp验证图片/*`、`poc验证图片/*`、`screenshots/*` | 仅记录；默认不单独上传，避免替换 zip |
| `video_paths` | `exp验证视频/*`、`poc验证视频/*`、`videos/*` | 仅记录；默认不单独上传，避免替换 zip |
| `browser_defaults` | 脚本推导 | 固化“0Day/原创=是或否”、二进制动态字段、附件策略等页面默认规则 |

## 2.1 NCC Word 模板字段

`scripts/prepare_ncc_material.py` 使用 `/Users/yao/Documents/网安- AI应用开发/监管上报/NCC通用型漏洞报告模板.docx` 作为母版。字段填充规则如下：

| NCC 模板字段 | 来源/默认值 |
|--------------|-------------|
| 漏洞报告标题 | CNVD `漏洞名称` |
| 漏洞发现时间 | 准备脚本运行当天日期 |
| 漏洞技术类型 | `漏洞类型`/标题/描述映射到 NCC 详细分类 |
| 漏洞描述 | CNVD `漏洞描述` 或 CNNVD `漏洞简介`；清理固定 AI 前缀、Markdown 和代码块 |
| 漏洞危害 | 优先 `漏洞危害`；缺失时按标题、产品、厂商、详细分类执行 websearch；检索失败则按漏洞类型策略生成保守危害文本，禁止写“无/见附件” |
| 漏洞厂商全称 | `漏洞厂商/影响厂商/厂商名称`；为“其他”时从标题产品名推断 |
| 已知受影响产品及版本 | `影响产品 + 影响版本/受影响实体版本号`；会把“已在 x 版本复现”清洗成产品和版本 |
| 互联网资产证明/测绘语法/资产数量/测绘截图 | 材料无明确字段时写“无” |
| 完整 PoC 描述 | `漏洞验证过程/漏洞分析/漏洞验证`；缺失时写“见附件” |
| 触发条件 | `漏洞触发条件/触发条件`；缺失时写“见附件” |
| 基础环境搭建/漏洞触发操作 | 从验证过程提取；无法拆分时环境写“见附件”，触发操作写验证过程 |
| 复现注意事项/黑盒案例/其他受影响目标 | 材料无明确字段时写“无” |
| 修复方案 | `修复方案/临时解决方案/正式解决方案`；缺失时按标题、产品、厂商、详细分类执行 websearch；检索失败则按漏洞类型策略生成修复建议，禁止写“见附件” |
| 备注 | `本材料由 CNVD/CNNVD 原始材料整理生成，证明材料见附件。` |

## 3. 当前填写规则

| 平台字段 | 规则 |
|----------|------|
| 是否0Day漏洞 | 使用 `is_0day`；准备阶段只问一次绑定值，并用 `--is-0day-original 是/否` 明确写入，给“是”就选“是”，给“否”就选“否” |
| 是否为原创漏洞 | 使用 `is_original`；与 `is_0day` 绑定一致，给“是”就选“是”，给“否”就选“否” |
| 发现日期 | 以页面允许的日期控件或当前材料确认，不直接使用 `提交日期` 硬填 |
| 漏洞业务类型/漏洞类型 | 固定保持/选择 `business_type=通用型漏洞`，不要选“事件型漏洞” |
| 影响对象 | 优先根据 `target_type` 选择 |
| 厂商所属国家/地区 | 使用 `vendor_country`；无法判断时为“中国大陆” |
| 漏洞厂商 | 使用 `unit_name` |
| 影响组件 | 使用 `affected_product` |
| 影响版本 | 使用 `version` |
| 漏洞名称 | 使用 `title` |
| 漏洞详细分类 | 使用 `detail_category`；二进制溢出类固定优先选“二进制”，不要保留平台默认“SQL注入” |
| 漏洞 URL | 使用 `url`；如果材料里就是 `http://none`，保持原样，不要自行改写 |
| 漏洞描述 | 使用清洗后的 `description`，不保留 Markdown 标记、代码块和“经恒脑AI代码审计智能体分析：”前缀 |
| 漏洞危害 | 使用 `impact`，准备阶段必须已补齐，禁止填写“见附件” |
| 修复方案 | 根据 `solution_type` 选择；修复方案说明使用 `formal_solution` 或 `temporary_solution`，准备阶段必须已补齐，禁止填写“见附件” |
| 详细分类联动字段 | 选择“漏洞详细分类”后按第 7 节映射表主动补齐；`版本号` 用 `version`，PoC/触发位置/利用工具等文本统一填“见附件”，联动下拉统一选“其他” |
| 漏洞附件 | 默认只上传 `upload_zip_path`；不要补传 docx/截图/视频，避免单文件控件替换 zip |

## 4. 影响对象允许选项

`target_type` 只能使用以下 NCC 页面选项。脚本会优先使用 Word 中的合法值；否则根据标题、产品名、漏洞类型推断。

操作系统、应用程序、WEB应用、数据库、网络设备（交换机、路由器等网络端设备）、安全产品、智能设备（物联网终端设备）、区块链公链、区块链联盟链、区块链外围系统、车联网、工业控制系统。

## 5. 厂商国家/地区规则

`vendor_country` 对应页面的厂商国家/地区类下拉。脚本会优先读取 Word 中的 `厂商所属国家`、`产品厂商国家`、`所属国家`、`国家/地区` 等字段；否则按厂商/产品关键词判断。无法可靠判断时默认“中国大陆”。

常见自动判断：Apache、ActiveMQ、APISIX、SeaTunnel、dotCMS 等优先“美国”；D-Link 优先“中国台湾”；DataGear、UJCMS、novel-plus、rebuild、安恒相关默认“中国大陆”。

## 6. 漏洞详细分类允许选项

`detail_category` 只能使用以下 NCC 页面选项。脚本会优先使用材料里已经给出的合法值；否则按标题、描述、漏洞危害中的关键词映射；无法判断时使用“其他”。

| 允许选项 | 典型匹配依据 |
|----------|--------------|
| SQL注入 | SQL 注入、SQLi、JDBC/查询注入 |
| XML实体注入 | XXE、XML 外部实体 |
| XSS | XSS、跨站脚本 |
| SSRF | SSRF、服务端请求伪造 |
| CSRF | CSRF、跨站请求伪造 |
| 弱口令 | 弱口令、默认口令、默认密码 |
| 文件上传 | 文件上传、上传绕过、任意上传 |
| 信息泄露 | 信息泄露、敏感信息、数据泄露 |
| 未授权访问 | 未授权、越权访问、认证/权限绕过 |
| 逻辑缺陷 | 逻辑缺陷、业务逻辑漏洞 |
| 文件包含 | 文件包含、LFI、RFI |
| 命令执行 | 命令执行、代码执行、RCE |
| 目录遍历 | 目录遍历、路径遍历 |
| 任意文件下载 | 任意文件下载、文件下载 |
| 任意文件读取 | 任意文件读取、文件读取 |
| 拒绝服务 | 拒绝服务、DoS、DDoS、崩溃 |
| 二进制 | 二进制、越界、溢出、ASAN、内存、反序列化、UAF |
| 工控设备 | 工控、工业控制、PLC、SCADA |
| 服务参数注入 | 服务参数注入、参数注入 |
| 点击劫持 | 点击劫持、clickjacking |
| 其他 | 无法可靠映射时使用 |

完整选项列表：SQL注入、XML实体注入、XSS、SSRF、CSRF、弱口令、文件上传、信息泄露、未授权访问、逻辑缺陷、文件包含、命令执行、目录遍历、任意文件下载、任意文件读取、拒绝服务、二进制、工控设备、服务参数注入、点击劫持、其他。

## 7. 漏洞详细分类联动字段映射表

选择不同的「漏洞详细分类」后，页面会动态显示/隐藏额外必填字段。汇总如下（2026-05-29 实测）：

| 漏洞详细分类 | 联动字段 | 字段类型 | 必填 | 特殊行为 |
|-------------|---------|---------|:----:|---------|
| `SQL注入` | SQL注入Poc | radio组 (get/post) | 是 | 旁边出现"目录导航"示例文本（非必填） |
| `XML实体注入` | — | — | — | 无额外字段 |
| `XSS` | 触发xss的payload | 多行输入框 | 是 | |
| `SSRF` | — | — | — | 无额外字段 |
| `CSRF` | — | — | — | 无额外字段 |
| `弱口令` | 弱口令账号 | 单行输入框 | 是 | |
| `弱口令` | 弱口令密码 | 单行输入框 | 是 | |
| `文件上传` | — | — | — | 无额外字段 |
| `信息泄露` | — | — | — | 无额外字段 |
| `未授权访问` | — | — | — | 无额外字段 |
| `逻辑缺陷` | — | — | — | 无额外字段 |
| `文件包含` | — | — | — | 无额外字段 |
| `命令执行` | 类型 | 下拉框 | 是 | 联动下拉值选"其他" |
| `命令执行` | 中间件/框架 | 下拉框 | 是 | 联动下拉值选"其他" |
| `命令执行` | 利用工具 | 单行输入框 | 是 | 填"见附件" |
| `目录遍历` | — | — | — | 无额外字段 |
| `任意文件下载` | — | — | — | 无额外字段 |
| `任意文件读取` | — | — | — | 无额外字段 |
| `拒绝服务` | PoC | 多行输入框 | 是 | |
| `拒绝服务` | 触发过程 | 多行输入框 | 是 | |
| `二进制` | 版本号 | 单行输入框 | 是 | ⚠ **隐藏「漏洞URL」字段**，不需要填 |
| `二进制` | 触发位置 | 多行输入框 | 否 | |
| `二进制` | PoC | 多行输入框 | 是 | |
| `工控设备` | 触发位置 | 多行输入框 | 是 | |
| `工控设备` | PoC | 多行输入框 | 是 | |
| `服务参数注入` | — | — | — | 无额外字段 |
| `点击劫持` | — | — | — | 无额外字段 |
| `其他` | poc | 多行输入框 | 是 | |

**联动字段填写规则：**
- 联动下拉（类型、中间件/框架）统一选"其他"
- 命令执行的"类型"和"中间件/框架"必须选中并回读成功，不能因字段延迟渲染或当前值为"请选择类型"而跳过
- 联动文本框/文本域（利用工具、PoC、触发位置、触发过程、poc、触发xss的payload、弱口令账号/密码）统一填"见附件"
- SQL注入Poc radio 按材料实际情况选 get/post；下方真实 PoC 内容框是 CodeMirror，必须用 `document.querySelector('.CodeMirror').CodeMirror.setValue(poc_text)` 写入从 Word 提取的原始 HTTP 请求或 curl 命令。HTTP 请求示例格式：第一行为 `GET /path HTTP/1.1` 或 `POST /path HTTP/1.1`，后续逐行填写 `Host: example.com`、`User-Agent: ...`、`Accept-Encoding: gzip, deflate`、`Connection: close` 等 Header。没有现成 PoC 时填“见附件”，不要自动构造请求体。
- **二进制** 选完后漏洞URL会被隐藏，不需也不应填写；版本号填具体版本，触发位置和PoC填"见附件"

## 8. 不要硬填的内容

- 不要把 `提交日期` 当成 `发现日期` 自动写入。
- 不要保留页面默认“漏洞详细分类=SQL注入”；二进制溢出类必须切到“二进制”。
- 不要把 PoC 文本按 textarea 顺序硬填；必须按 label 填到“PoC”，触发位置填到“触发位置”。
- 不要自行编造厂商官网或验证过程；漏洞危害、修复方案说明必须来自准备阶段 `security_guidance`，浏览器阶段禁止临时写“见附件”。

## 8. 首次联调后需要补充

NCC 平台实际 `uid`、下拉可选值、提交成功页元素仍需在真实页面里用 MCP `take_snapshot` 确认，随后补充到：

- `references/selectors.md`
- 本文件的“当前填写规则”
